apiVersion: v1
kind: Service
metadata:
  name: books-service
  labels:
    app: books
spec:
  selector:
    app: books
  ports:
  - name: http
    protocol: TCP
    port: 3001
    targetPort: 3001
  type: NodePort
