apiVersion: v1
kind: Service
metadata:
  name: user-service
  labels:
    app: user
spec:
  selector:
    app: user
  ports:
  - name: http
    protocol: TCP
    port: 3002
    targetPort: 3002
  type: NodePort
