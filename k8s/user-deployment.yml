apiVersion: apps/v1
kind: Deployment
metadata:
  name: user-deployment
  labels:
    app: user
spec:
  replicas: 2
  selector:
    matchLabels:
      app: user
  template:
    metadata:
      labels:
        app: user
    spec:
      initContainers:
      - name: wait-for-db
        image: busybox:1.36
        command: ['sh', '-c', 'until nc -z db-service 5432; do echo "waiting for db"; sleep 2; done']
      containers:
      - name: user-container
        image: devopswithai.azurecr.io/user-service:latest
        imagePullPolicy: Always
        ports:
        - containerPort: 3002
        env:
        - name: DB_HOST
          valueFrom:
            configMapKeyRef:
              name: devopswithai-config
              key: DATABASE_HOST
        - name: DB_NAME
          valueFrom:
            configMapKeyRef:
              name: devopswithai-config
              key: DATABASE_NAME
        - name: DB_PORT
          valueFrom:
            configMapKeyRef:
              name: devopswithai-config
              key: DATABASE_PORT
        - name: DB_USER
          valueFrom:
            secretKeyRef:
              name: devopswithai-secret
              key: DB_USERNAME
        - name: DB_PASSWORD
          valueFrom:
            secretKeyRef:
              name: devopswithai-secret
              key: DB_PASSWORD
        readinessProbe:
          tcpSocket:
            port: 3002
          initialDelaySeconds: 10
          periodSeconds: 5
          failureThreshold: 3
        livenessProbe:
          tcpSocket:
            port: 3002
          initialDelaySeconds: 20
          periodSeconds: 10
          failureThreshold: 3
