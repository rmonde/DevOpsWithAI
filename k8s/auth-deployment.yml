apiVersion: apps/v1
kind: Deployment
metadata:
  name: auth-deployment
  labels:
    app: auth
spec:
  replicas: 2
  selector:
    matchLabels:
      app: auth
  template:
    metadata:
      labels:
        app: auth
    spec:
      initContainers:
      - name: wait-for-db
        image: busybox:1.36
        command: ['sh', '-c', 'until nc -z db-service 5432; do echo "waiting for db"; sleep 2; done']
      containers:
      - name: auth-container
        image: devopswithai.azurecr.io/auth-service:7d218b2cdbd244cbd2fa835991c6fe7ec055af9f
        imagePullPolicy: Always
        ports:
        - containerPort: 3000
        env:
        - name: DB_HOST
          valueFrom:
            configMapKeyRef:
              name: devopswithai-config
              key: DATABASE_HOST
        - name: DB_NAME
          valueFrom:
            configMapKeyRef:
              name: devopswithai-config
              key: DATABASE_NAME
        - name: DB_PORT
          valueFrom:
            configMapKeyRef:
              name: devopswithai-config
              key: DATABASE_PORT
        - name: DB_USER
          valueFrom:
            secretKeyRef:
              name: devopswithai-secret
              key: DB_USERNAME
        - name: DB_PASSWORD
          valueFrom:
            secretKeyRef:
              name: devopswithai-secret
              key: DB_PASSWORD
        readinessProbe:
          tcpSocket:
            port: 3000
          initialDelaySeconds: 10
          periodSeconds: 5
          failureThreshold: 3
        livenessProbe:
          tcpSocket:
            port: 3000
          initialDelaySeconds: 20
          periodSeconds: 10
          failureThreshold: 3
